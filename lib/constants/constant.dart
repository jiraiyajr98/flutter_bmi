import 'package:flutter/material.dart';

const TextStyle kTextStyle =
    TextStyle(fontSize: 18.0, color: Color(0xFF8D8E98));

const TextStyle kCalculateText =
    TextStyle(fontWeight: FontWeight.w600, fontSize: 20.0, color: Colors.white);

const TextStyle kResultStyle =
TextStyle(fontSize: 20.0, fontWeight: FontWeight.w500,letterSpacing: 2.0);

const TextStyle kNumberStyle =
    TextStyle(fontSize: 50.0, fontWeight: FontWeight.w900);

const double kMinHeight = 120.0;
const double kMaxHeight = 320.0;
const double kButtonHeight = 60.0;
const Color kDefaultNotSelected = Color(0xFF141624);
const Color kDefaultSelected = Color(0xFF343547);
const Color kInActiveColor = Color(0xFF8D8E98);
const Color kAccentColor = Color(0xFFEA1556);
enum Gender { Male, Female }
enum Weight { subWeight, addWeight }
enum Age { subAge, addAge }
